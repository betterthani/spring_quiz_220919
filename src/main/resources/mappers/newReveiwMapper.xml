<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.quiz.lesson05.dao.NewReviewDAO">
 	<select id="selectNewReviewByIdName" resultType="com.quiz.lesson05.model.NewReview">
 		SELECT
	 		A.name, B.userName, B.point, B.createdAt, B.review, B.menu
 		FROM `store` AS A
		JOIN `new_review` AS B
		ON A.id = B.storeId
 		WHERE
 			B.storeId = #{storeId}
 		AND
 			A.name = #{name}
 	</select>
 </mapper>